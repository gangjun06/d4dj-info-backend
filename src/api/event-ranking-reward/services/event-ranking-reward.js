'use strict';

/**
 * event-ranking-reward service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::event-ranking-reward.event-ranking-reward');
