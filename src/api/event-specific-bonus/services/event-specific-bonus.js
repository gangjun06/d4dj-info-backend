'use strict';

/**
 * event-specific-bonus service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::event-specific-bonus.event-specific-bonus');
