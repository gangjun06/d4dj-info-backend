'use strict';

/**
 * event-specific-bonus router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::event-specific-bonus.event-specific-bonus');
