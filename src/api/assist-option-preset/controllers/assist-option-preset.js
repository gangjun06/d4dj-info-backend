'use strict';

/**
 *  assist-option-preset controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::assist-option-preset.assist-option-preset');
