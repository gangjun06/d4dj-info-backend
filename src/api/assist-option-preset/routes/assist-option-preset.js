'use strict';

/**
 * assist-option-preset router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::assist-option-preset.assist-option-preset');
