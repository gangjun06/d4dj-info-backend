'use strict';

/**
 * assist-option-preset service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::assist-option-preset.assist-option-preset');
